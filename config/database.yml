default: &default
  adapter: postgresql
  encoding: utf8
  pool: 5
  username: postgres
  password: password

development:
  <<: *default
  database: sql-demo-dev
  host: localhost

test:
  <<: *default
  database: sql-demo-test
  host: localhost
